/**
 * ⚠️  DEPRECATED — DO NOT USE
 * ─────────────────────────────────────────────────────────────────────
 * This script has been replaced by the in-app Ward Import Console.
 *
 * Navigate to:  /admin/ward-import
 *
 * The new page provides:
 *   • JSON / GeoJSON file upload
 *   • Preview before import
 *   • Duplicate ward ID detection
 *   • Live per-ward import progress
 *   • Success / failure report
 *
 * No service account, no Firebase Admin SDK, no Cloud Functions needed.
 * Uses the existing authenticated GMC Admin session (Firebase Web SDK).
 * ─────────────────────────────────────────────────────────────────────
 */

console.error('This script is deprecated. Use the in-app Ward Import page at /admin/ward-import');
process.exit(1);
